// Publishing packages is an important part of package management, but it comes
// after everything else, not before.
/*
export class Project extends Dependency {
    // TODO: Make spdx license thing
    public constructor(name: string,
                       version: string,
                       public author: string,
                       public dependencies: [Dependency],
                       public synopsis: string =
                        "Short description of your project",
                       public description: string =
                        "Longer description of your project",
                       public license: string = "MIT",
                       public repository: string = "") {
        super(name, version)
    }
}
*/
